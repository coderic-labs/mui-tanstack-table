import { Meta } from '@storybook/blocks';

<Meta title="Apis/Components/Table" />

# Table

## Usage description

Render a fully configured TanStack table instance using MUI table primitives.

## Shape

```ts
Table<T>(props: TableProps<T>)
```

## Props

### Type definition

```ts
type TableProps<T> = MuiTableProps & {
  table: TanstackTable<T>;
  rowDetail?: RowDetailComponent<T>;
  getRowStyle?: GetRowStyle<T>;
  stickyFooter?: boolean;
  tableLayout?: 'auto' | 'fixed';
};
```

### Own props

- `table`
  Type: `TanstackTable<T>`
  Required TanStack table instance used to render headers, rows, and optional footer.
- `rowDetail`
  Type: `RowDetailComponent<T> | undefined`
  Optional renderer for expanded row detail content.
- `getRowStyle`
  Type: `GetRowStyle<T> | undefined`
  Optional callback for per-row style customization.
- `stickyFooter`
  Type: `boolean | undefined`
  Optional flag to keep footer cells sticky at the bottom when the table is rendered in a scrollable container.
- `tableLayout`
  Type: `'auto' | 'fixed' | undefined`
  Optional table layout mode. Defaults to `'auto'`. Use `'fixed'` to enable column sizing funcs.

## Related demos

- [Client Side related demo](?path=/story/demo-client-side-table--client-side-table)
- [Server Side related demo](?path=/story/demo-server-side-table--server-side-table)
