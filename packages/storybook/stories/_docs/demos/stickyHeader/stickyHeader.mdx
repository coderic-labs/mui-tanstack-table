import { Meta, Canvas, Story } from '@storybook/blocks';
import * as Stories from './stickyHeader.stories.tsx';

<Meta of={Stories} />

# Sticky Header

This demo shows how to keep table headers visible while scrolling by enabling `stickyHeader` on `Table`.

## Example

<Canvas>
  <Story of={Stories.Demo} />
</Canvas>
