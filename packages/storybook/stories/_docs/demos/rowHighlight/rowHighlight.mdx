import { Meta, Canvas, Story } from '@storybook/blocks';
import * as Stories from './rowHighlight.stories.tsx';

<Meta of={Stories} />

# Row Highlight

This demo shows how to use `getCellStyle` to highlight rows based on data values. Intern rows are highlighted in warning color, part-time rows in info color, and full-time rows have no highlight.

## Example

<Canvas>
  <Story of={Stories.Demo} />
</Canvas>
