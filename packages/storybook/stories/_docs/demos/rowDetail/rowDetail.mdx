import { Meta, Canvas, Story } from '@storybook/blocks';
import * as Stories from './rowDetail.stories.tsx';

<Meta of={Stories} />

# Row Detail

This demo shows expandable rows using `TableRowExpansionHeader`, `TableRowExpansionCell`, and the `rowDetail` prop on `Table`.

## Example

<Canvas>
  <Story of={Stories.Demo} />
</Canvas>
