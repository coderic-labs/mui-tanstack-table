import { Meta, Canvas, Story } from '@storybook/blocks';
import * as Stories from './rowSelection.stories.tsx';

<Meta of={Stories} />

# Row Selection

This demo shows row selection using the same table wiring for both client-side and server-side tables.

## Example

<Canvas>
  <Story of={Stories.Demo} />
</Canvas>
